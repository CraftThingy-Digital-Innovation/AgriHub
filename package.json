{
  "name": "agrihub-indonesia",
  "version": "1.0.0",
  "private": true,
  "description": "Platform Digitalisasi Ketahanan Pangan Indonesia",
  "scripts": {
    "dev": "npx --yes concurrently --names \"BACKEND,FRONTEND\" --prefix-colors \"green,cyan\" \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "npm run dev --prefix apps/backend",
    "dev:frontend": "npm run dev --prefix apps/frontend",
    "install:all": "npm install --prefix apps/backend --legacy-peer-deps && npm install --prefix apps/frontend --legacy-peer-deps",
    "build:frontend": "npm run build --prefix apps/frontend",
    "build:backend": "npm run build --prefix apps/backend"
  },
  "engines": {
    "node": ">=20.0.0",
    "npm": ">=10.0.0"
  }
}
